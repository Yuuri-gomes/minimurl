services:
  db:
    image: mysql/mysql-server:latest
    container_name: "minimurl_db_local"
    restart: always
    env_file:
      - ../.env
    ports:
      - "3306:3306"
    expose:
      - "3306"
    volumes:
      - mysql-volume:/var/lib/mysql
volumes:
  mysql-volume:
    driver: local
