const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'infra', 'config', 'sequelizeConnectionConfig.js'),
  'migrations-path': path.resolve(__dirname, 'infra', 'migrations')
}