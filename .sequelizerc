require("babel-register");
const path = require("path");

module.exports = {
  config: path.resolve(
    __dirname,
    "infra",
    "config",
    "sequelize-connection-config.js",
  ),
  "migrations-path": path.resolve(__dirname, "infra", "migrations"),
};
